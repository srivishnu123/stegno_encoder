# stegno_encoder
Developed a Python-based steganography tool that hides secret text inside images using the LSB technique. Implemented modular encoding logic with bit-level manipulation while preserving image quality. Ensured secure message embedding with clear user flow and error handling.
